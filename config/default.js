module.exports = Object.freeze({
  globalRoot: `${process.cwd()}`,
  publicRoot: `${process.cwd()}/public`,
  filesRoot: `${process.cwd()}/files`,
  indexPath: `${process.cwd()}/public/index.html`,
  testRoot: `${process.cwd()}/test`,
  faviconPath: `${process.cwd()}/favicon.ico`,
  fileSizeLimit: 1e6
});
